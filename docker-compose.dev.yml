services:
  app:
    build:
      target: dev
    container_name: dexterman-app
    command: pnpm run docker:dev
    volumes:
      - ./src:/app/src
      - ./data:/app/data
      - ./public:/app/public
      - ./scripts:/app/scripts
      - ./tsconfig.json:/app/tsconfig.json:ro
      - ./package.json:/app/package.json:ro
      - ./pnpm-lock.yaml:/app/pnpm-lock.yaml:ro
    environment:
      - CHOKIDAR_USEPOLLING=true
